# Notes on the ZDE source material

Hex dumping or disassembling `zde17.com` is allowed if ever needed, but
prefer the `zde17.asm` source when possible, since no disassembler is
currently installed. (`zde17.com` itself is not currently present in this
tree.)
